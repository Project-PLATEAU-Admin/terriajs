describe("ClippingPlanesController", function() {
  it("fires a pick event when a plane is picked");
  it("fires a release event when a plane is released");
  it("fires a move event when a plane is moved");
  it("fires the move event with correct moveAmount");
  it("fires a mouseOver event when the cursor is above a plane");
  it("fires a mouseOut event when the cursor is leaves the plane");
  it("should not fire a mouseOver event when a plane is already picked");
});
